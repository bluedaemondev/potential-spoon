using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace UILibrary
{
    public class LineUiRenderer : MonoBehaviour
    {
        [SerializeField]
        List<Transform> pointsToConnect = new List<Transform>();
        LineRenderer lineRenderer;


        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}